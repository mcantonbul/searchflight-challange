import { IThema } from "@/models";

const ResultThema: IThema = {
    backgroundColor: 'white',
    header: {
        color: 'black'
    },
    footer: {
        color: 'black'
    }
};

export default ResultThema;
