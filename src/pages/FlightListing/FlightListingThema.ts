import { IThema } from "@/models";

const FlightListingThema: IThema = {
    backgroundColor: 'white',
    header: {
        color: 'black'
    },
    footer: {
        color: 'black'
    }
};

export default FlightListingThema;
